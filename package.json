{
  "name": "ritter-jekyll",
  "version": "0.1.1",
  "description": "The Ritter Jekyll Experience",
  "bin": "lib/index.js",
  "files": [
    "lib/"
  ],
  "keywords": [
    "ritter",
    "jekyll"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ritterim/ritter-jekyll"
  },
  "scripts": {
    "audit": "nsp check package",
    "build": "babel -d ./ ./src && shx cp ./src/lib/*.json ./lib/",
    "check": "npm run audit && npm outdated --depth 0",
    "coverage": "nyc --reporter=lcov --reporter=text --reporter=html npm test",
    "lint": "eslint src test",
    "test": "ava --require babel-core/register",
    "integration-test": "npm run build && node ./lib/index.js",
    "validate": "npm run lint && npm test"
  },
  "author": {
    "name": "Ritter Insurance Marketing",
    "url": "https://github.com/ritterim"
  },
  "pre-commit": [
    "validate"
  ],
  "dependencies": {
    "glob": "^7.0.5",
    "globby": "^6.0.0",
    "imagemin-cli": "^3.0.0",
    "jpeg-recompress-bin": "^3.0.1",
    "markdown-proofing": "^0.2.0",
    "npm-run": "^4.1.0"
  },
  "devDependencies": {
    "ava": "^0.16.0",
    "babel-cli": "^6.5.1",
    "babel-core": "^6.5.2",
    "babel-eslint": "^6.0.0",
    "babel-plugin-transform-function-bind": "^6.5.2",
    "babel-preset-es2015": "^6.5.0",
    "eslint": "^3.3.1",
    "nsp": "^2.2.0",
    "nyc": "^8.1.0",
    "pre-commit": "^1.1.2",
    "shx": "^0.1.4"
  }
}
